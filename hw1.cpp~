#include <iostream>
#include <fstream>
#include <cstdlib>
using namespace std;
int main(){
    ifstream in;
    ofstream out;

    in.open("samples/01.txt");
    if(in.fail()){
        cout << "input file opening failed";
        exit(1);
    }
    out.open("output.txt");
    if(out.fail()){
        cout << "output file opening failed";
        exit(1);
    }
    in.close();
    out.close();
    return 0;
}

